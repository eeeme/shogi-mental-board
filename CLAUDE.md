# CLAUDE.md

将棋の脳内盤トレーニングアプリ。自分専用のトレーニングツールとして開発する（当面）。
このファイルは Claude Code が毎回読むプロジェクト規約。簡潔に保つこと。

## プロダクト概要

盤を見ずに座標・手順を扱う力（脳内盤）を鍛えるドリルアプリ。ホーム画面から機能を選ぶ。

機能（8つ）:
1. 読み上げ→マス押下（逐次1問1答）: 座標を1つ提示（符号表示 and/or 音声）→ 該当マスを即タップ → 採点 → 次の1問。符号と音声のどちらか一方をオフにできる（両方オフ不可・盤面は常時オン）。**誤答時は「タップしてしまったマス」をエラー色で表示し、正解マスは正解色のまま同時に見せる**
2. マスが光る→記号回答（逆変換）: ハイライトされたマスの座標を答える
3. 系列記憶（順番タップ→答え合わせ）: 座標を N個順に提示 → 記憶 → 示された順に N個タップ → 最後に答え合わせ。N は設定可能。提示手段（盤面ハイライト/符号表示/音声）のうちオフにでき、1つは必ずオン。※旧「ただ読み上げ」を再定義（音声だけオンにした状態がそれに相当）
4. ランダム配置記憶: 先手・後手が交互にランダムなマスへ駒を打つ → 盤を隠し、最終盤面を位置＋向き＋駒種まで再現・答え合わせ。枚数は設定可能。駒の移動・合法手生成・AIは使わない。回答UIが重いため初回ローンチには含めず後続フェーズで実装
5. 詰将棋: 配置を音声で読み、見ずに解く
6. 統計: 正答率・反応時間・日別推移・連続日数
7. 支援（お涙頂戴）: 将来の投げ銭リンク置き場（今はプレースホルダ）
8. ただ読み上げ（受動）: 座標をひたすら音声で読み上げ続けるだけ。**答え合わせなし・タップなし**。ながら聴き用。停止するまで継続

MVPの範囲は 1・2・3・6（統計）＋ 8。4・5・7は後続フェーズ。

## 技術スタック

- React + TypeScript + Vite
- PWA（インストール可能・オフライン動作）
- Tailwind CSS
- 状態管理: Zustand（軽量）
- 音声: Web Speech API（`speechSynthesis`）
- 永続化: IndexedDB（セッション記録）/ localStorage（設定）
- **バックエンドなし。全機能クライアント完結。** サーバー/DB/API を追加しないこと。

将来: Capacitor でネイティブ化して iOS/Android ストア展開。今はその道を塞がない設計に留め、ネイティブ依存を先取りしない。

## 開発コマンド

```bash
npm install
npm run dev        # ローカル開発サーバ
npm run build      # 本番ビルド
npm run preview    # ビルド結果をローカル確認
npm run test       # ユニットテスト（Vitest）
npm run lint       # ESLint
npm run typecheck  # tsc --noEmit
```

## ディレクトリ構造（想定）

```
src/
  components/
    ShogiBoard.tsx      # 9x9盤。タップ検出・マスのハイライト・駒描画
  lib/
    coords.ts           # 筋段 ⇔ index ⇔ 読み仮名 の変換（純粋関数）
    tts.ts              # 符号読み変換 + speechSynthesis ラッパー
    storage.ts          # IndexedDB / localStorage アクセス
  features/
    tapCell/            # ① 読み上げ→マス押下（逐次1問1答）
    reverse/            # ② 逆変換
    sequence/           # ③ 系列記憶（順番タップ→答え合わせ）
    listen/             # ⑧ ただ読み上げ（受動・答え合わせなし）
    recall/             # ④ ランダム配置記憶
    tsume/              # ⑤ 詰将棋
    stats/              # ⑥ 統計
    support/            # ⑦ 支援
  store/                # Zustand ストア
  pages/                # Home / Settings など
  App.tsx
```

## ドメイン規約（重要・間違えやすい）

### 将棋の座標系
- **筋（すじ）= 縦の列。右から左へ 1〜9。**
- **段（だん）= 横の行。上から下へ 一〜九（1〜9）。**
- マス表記は「筋段」の順（例: 7六 = 7筋6段）。
- 内部表現は `{ file: 1..9, rank: 1..9 }`。
- **盤の向き（先手視点/後手視点）は各モードの設定画面で切り替える。**（アプリ全体設定からは外す。二重管理を避けるため設定の持ち主はモード側。値はモードごとに永続化）
- **座標番号ラベルの位置**（将棋の標準表記に準拠）:
  - 先手視点: 筋（9〜1）を**上端**、段（一〜九）を**右端**
  - 後手視点: 盤を180°回した形。筋を**下端**、段を**左端**
  - ラベルの表示オン/オフも各モードの設定画面で切替可能にする。

### 盤の見た目・操作性
- **星（ほし）**: 実際の将棋盤にある4つの点を再現する。位置は**敵陣の境界＝3段目/4段目の罫線と、3筋/4筋・6筋/7筋の罫線が交わる4つの交点**。飾りではなく敵陣境界の目印であり位置感覚のアンカーになるため実装する。ただし墨系の低コントラストで描き、ハイライト（光る一マス）と competing させない。
- **タップ標的**: ミスタップ防止のため、1マスの実効タップ領域を最低 44px 以上確保する。盤の外周余白を詰めて盤を画面幅いっぱいまで広げ、必要なら**見た目のマスより広い不可視ヒットエリア**を持たせる。

### 符号の読み上げ（全機能の土台）
- TTS に `"7六"` や `"76"` をそのまま渡さない。**ひらがなの読み（例 `"ななろく"`）に変換して渡す。**
- 読みの流派を設定で切替: 4=「よん/し」, 7=「なな/しち」, 9=「きゅう/く」。既定は modern（よん・なな・きゅう）。
- **2と5は伸ばす**: 1〜9のうち1モーラなのは「に」「ご」だけで、TTSだと音が飲まれて聞き取れない。**2=「にぃ」, 5=「ごぅ」**として読ませる（筋・段の両方）。実機で不自然なら長音符版「にー」「ごー」にフォールバックしてよい（実機確認して良い方を採用）。
- 変換は `coords.ts` の純粋関数に集約し、必ずユニットテストを書く。読みを変えたらテストも更新すること。

### iOS Safari の TTS 制約
- 音声はユーザー操作を起点にしないと再生されない。各機能は「開始ボタンを踏む → 発話開始」の流れを必須にする。

## コーディング規約

- TypeScript strict。`any` を避ける。
- コンポーネントは関数コンポーネント + Hooks。
- 純粋ロジック（coords, 集計）は UI から分離し、テスト可能に保つ。
- 命名は英語、UI表示テキストは日本語。
- 副作用（TTS・ストレージ）はラッパー経由。コンポーネントから直接 `window.speechSynthesis` を触らない。

## テスト方針

- `coords.ts`（座標⇔読み仮名）と統計集計ロジックは**必ずユニットテスト**（Vitest）。
- UIは最小限のスモークテストで可。ロジックにカバレッジを寄せる。

## Git / PR 運用

- Linear の Issue 単位で作業。ブランチ名に Issue キーを含める（例: `feat/SHO-12-listen-mode`）。
- 1機能 = 1PR を基本に小さく刻む。
- PR には動作確認手順（スマホブラウザでの確認含む）を書く。

## 参照

- 全体設計・フェーズ計画: `docs/design.md`
- デザイン方針: `docs/design-brief.md`

## やらないこと

- バックエンド/外部APIの追加（自分専用のうちは不要）。
- localStorage/IndexedDB 以外の永続化。
- 課金・広告の実装（将来フェーズ。MVPに入れない）。
- ネイティブ専用APIへの依存（PWAで動く範囲に留める）。
