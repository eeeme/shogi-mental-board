# CLAUDE.md

将棋の脳内盤トレーニングアプリ。自分専用のトレーニングツールとして開発する（当面）。
このファイルは Claude Code が毎回読むプロジェクト規約。簡潔に保つこと。

## プロダクト概要

盤を見ずに座標・手順を扱う力（脳内盤）を鍛えるドリルアプリ。ホーム画面から機能を選ぶ。

モード（ホームのカード。番号は表示名にも使う）:
- **00 ただ読み上げ（受動）**: 座標をひたすら音声で読み上げ続けるだけ。タップも答え合わせもなし。停止するまで継続。ながら聴き用。**このモードは将来も無料**（課金対象外）
- **01 読み上げ→マス押下（逐次1問1答）**: 座標を1つ提示（符号表示 and/or 音声）→ 該当マスを即タップ → 採点 → 次の1問。符号と音声のどちらか一方をオフにできる（両方オフ不可・盤面は常時オン）。誤答時は「タップしてしまったマス」をエラー色で表示し、正解マスは正解色のまま同時に見せる
- **02 マスが光る→記号回答（逆変換）**: ハイライトされたマスの座標を答える
- **03 系列記憶（順番タップ→答え合わせ）**: 座標を N個順に提示 → 記憶 → 示された順に N個タップ → 最後に答え合わせ。N は設定可能。提示手段（盤面ハイライト/符号表示/音声）のうちオフにでき、1つは必ずオン
- **04 ランダム配置記憶**: 先手・後手が交互にランダムなマスへ駒を打つ → 盤を隠し、最終盤面を位置＋向き＋駒種まで再現・答え合わせ。枚数は設定可能。駒の移動・合法手生成・AIは使わない。回答UIが重いため初回ローンチには含めず後続フェーズ
- **05 詰将棋**: 配置を音声で読み、見ずに解く。後続フェーズ

タブ（モードではない。ボトムナビ）:
- ホーム / 統計 / 問い合わせ / 設定
- **統計**: 正答率・反応時間・日別推移・連続日数。※モードカードとしては置かない
- **問い合わせ**: Googleフォームへの導線。**支援（投げ銭）もこのタブに置く**。※支援もモードカードとしては置かない

MVPの範囲は 00・01・02・03 ＋ 統計タブ。04・05は後続フェーズ。

## 技術スタック

- React + TypeScript + Vite
- PWA（インストール可能・オフライン動作）
- Tailwind CSS
- 状態管理: Zustand（軽量）
- 音声: Web Speech API（`speechSynthesis`）
- 永続化: IndexedDB（セッション記録）/ localStorage（設定）
- **バックエンドなし。全機能クライアント完結。** サーバー/DB/API を追加しないこと。

将来: Capacitor でネイティブ化して iOS/Android ストア展開。今はその道を塞がない設計に留め、ネイティブ依存を先取りしない。

## 開発コマンド

```bash
npm install
npm run dev        # ローカル開発サーバ
npm run build      # 本番ビルド
npm run preview    # ビルド結果をローカル確認
npm run test       # ユニットテスト（Vitest）
npm run lint       # ESLint
npm run typecheck  # tsc --noEmit
```

## ディレクトリ構造（想定）

```
src/
  components/
    ShogiBoard.tsx      # 9x9盤。タップ検出・マスのハイライト・駒描画
  lib/
    coords.ts           # 筋段 ⇔ index ⇔ 読み仮名 の変換（純粋関数）
    tts.ts              # 符号読み変換 + speechSynthesis ラッパー
    storage.ts          # IndexedDB / localStorage アクセス
  features/
    listen/             # 00 ただ読み上げ（受動・答え合わせなし・無料）
    tapCell/            # 01 読み上げ→マス押下（逐次1問1答）
    reverse/            # 02 逆変換
    sequence/           # 03 系列記憶（順番タップ→答え合わせ）
    recall/             # 04 ランダム配置記憶
    tsume/              # 05 詰将棋
  pages/
    Home.tsx            # モードカード一覧（00〜05）
    Stats.tsx           # 統計タブ
    Contact.tsx         # 問い合わせタブ（Googleフォーム導線＋支援）
    Settings.tsx        # 設定タブ
  store/                # Zustand ストア
  App.tsx
```

## ドメイン規約（重要・間違えやすい）

### 将棋の座標系
- **筋（すじ）= 縦の列。右から左へ 1〜9。**
- **段（だん）= 横の行。上から下へ 一〜九（1〜9）。**
- マス表記は「筋段」の順（例: 7六 = 7筋6段）。
- 内部表現は `{ file: 1..9, rank: 1..9 }`。
- **盤の向き（先手視点/後手視点）は各モードの設定画面で切り替える。**（アプリ全体設定からは外す。二重管理を避けるため設定の持ち主はモード側。値はモードごとに永続化）
- **座標番号ラベルの位置**（将棋の標準表記に準拠）:
  - 先手視点: 筋（9〜1）を**上端**、段（一〜九）を**右端**
  - 後手視点: 盤を180°回した形。筋を**下端**、段を**左端**
  - ラベルの表示オン/オフも各モードの設定画面で切替可能にする。

### 盤の見た目・操作性
- **星（ほし）**: 実際の将棋盤にある4つの点を再現する。位置は「3段目と4段目を分ける罫線」「6段目と7段目を分ける罫線」× 「3筋と4筋を分ける罫線」「6筋と7筋を分ける罫線」の交点、計4点。敵陣境界の目印であり位置感覚のアンカーになるため実装する。墨系の低コントラストで小さく描き、ハイライト（光る一マス）と競合させない。
- **座標番号ラベルの整列**: 段の漢数字（一〜九）が対応する行と**必ず垂直方向に中心が揃う**こと。筋の数字も同様に列と揃える。現状ズレているため要修正（ラベルとマスを同じグリッドで組み、別レイヤーで絶対配置しない）。
- **タップ標的**: ミスタップ防止のため、1マスの実効タップ領域を最低 44px 以上確保する。盤の外周余白を詰めて盤を画面幅いっぱいまで広げ、必要なら**見た目のマスより広い不可視ヒットエリア**を持たせる。

### 符号の読み上げ（全機能の土台）
- TTS に `"7六"` や `"76"` をそのまま渡さない。**読み仮名に変換して渡す。**
- **読み仮名はカタカナで渡す**（例 `"ナナロク"`）。ひらがなだと日本語TTSが文頭の「は」を助詞と解釈して "wa" と読むため、`"はちきゅう"` が「わちきゅう」になる不具合が出る。カタカナにすると回避できる。
- 読みの流派を設定で切替: 4=「ヨン/シ」, 7=「ナナ/シチ」, 9=「キュウ/ク」。既定は modern（ヨン・ナナ・キュウ）。
- 8 は必ず「ハチ」と読ませる（「ワチ」にならないこと）。
- **2と5は伸ばす**: 1〜9のうち1モーラなのは「ニ」「ゴ」だけで、TTSだと音が飲まれて聞き取れない。**2=「ニー」, 5=「ゴー」**として読ませる（筋・段の両方）。実機で不自然なら「ニィ」「ゴゥ」にフォールバックしてよい（実機確認して良い方を採用）。
- 変換は `coords.ts` の純粋関数に集約し、必ずユニットテストを書く。読みを変えたらテストも更新すること。

### ホームのモード説明モーダル
- モードカードをタップしたら、**ホーム画面上でポップアップを表示**する（モード画面や設定画面に遷移してから出すのではない）。
- 内容: そのモードの短い説明＋動くデモ（実物の `ShogiBoard` を使った自動再生。GIF画像は使わない）。
- ボタン: 「はじめる」→ モード画面へ遷移 / 「×」→ **閉じてホーム画面のまま**（遷移しない）。
- 将来この位置が課金導線になる（一部モードは買い切りアンロック）。**ポップアップで内容を見せてから購入を促す**設計にするため、モーダル下部に課金CTAを差し込める余地を残しておくこと。
- 「次回から表示しない」は付けない（課金導線を兼ねるため毎回表示）。ただしデモの自動再生は `prefers-reduced-motion` を尊重する。

### iOS Safari の TTS 制約
- 音声はユーザー操作を起点にしないと再生されない。各機能は「開始ボタンを踏む → 発話開始」の流れを必須にする。

## コーディング規約

- TypeScript strict。`any` を避ける。
- コンポーネントは関数コンポーネント + Hooks。
- 純粋ロジック（coords, 集計）は UI から分離し、テスト可能に保つ。
- 命名は英語、UI表示テキストは日本語。
- 副作用（TTS・ストレージ）はラッパー経由。コンポーネントから直接 `window.speechSynthesis` を触らない。

## テスト方針

- `coords.ts`（座標⇔読み仮名）と統計集計ロジックは**必ずユニットテスト**（Vitest）。
- UIは最小限のスモークテストで可。ロジックにカバレッジを寄せる。

## Git / PR 運用

- Linear の Issue 単位で作業。ブランチ名に Issue キーを含める（例: `feat/SHO-12-listen-mode`）。
- 1機能 = 1PR を基本に小さく刻む。
- PR には動作確認手順（スマホブラウザでの確認含む）を書く。

## 参照

- 全体設計・フェーズ計画: `docs/design.md`
- デザイン方針: `docs/design-brief.md`

## やらないこと

- バックエンド/外部APIの追加（自分専用のうちは不要）。
- localStorage/IndexedDB 以外の永続化。
- 課金・広告の実装（将来フェーズ。MVPに入れない）。
- ネイティブ専用APIへの依存（PWAで動く範囲に留める）。
